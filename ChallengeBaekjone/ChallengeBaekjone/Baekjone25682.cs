using System;
using System.IO;
using System.Linq;

namespace ChallengeBaekjone
{
    public class Baekjone25682
    {
        static void Problem()
        {
            var sr = new StreamReader(Console.OpenStandardInput());
            var sw = new StreamWriter(Console.OpenStandardOutput());

            sr.Close();
            sw.Close();
        }
    }
}